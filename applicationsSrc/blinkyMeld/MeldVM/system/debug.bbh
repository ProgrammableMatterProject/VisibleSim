#ifndef __DEBUG_H__
#define __DEBUG_H__

#include <stdio.h>
//#include <avr/io.h>

//  DEBUG 
//
//	This binds the bottom uart (with the male header) to stdout so printf() has somewhere to go.
//  Defaults to 115200 baud unless init_debug is called before any init_port or init_uart().
// 
//  Note: outputting non-printable characters on the port, or trying to use the bottom as normal not recommended.
void initDebug(void);

#endif
